<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tpf.Dao.UserMapper">


    <select id="getAll" resultType="com.tpf.Entity.User">
        select * from user
    </select>

    <select id="getUserById" resultType="com.tpf.Entity.User">

        select * from user where uid=#{id}
    </select>

    <select id="getUserByNameAndPassword" resultType="com.tpf.Entity.User">
        select * from user where uname=#{uname} and upassword=#{upassword}

    </select>

    <select id="getUserByName" resultType="com.tpf.Entity.User">
                select * from user where uname=#{uname}
    </select>


</mapper>


